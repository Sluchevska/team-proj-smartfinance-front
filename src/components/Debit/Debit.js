import React from 'react';
import { Wrapper } from './Debit.styled';
import Container from './Container';
import { Title } from './Debit.styled';

const Debit = () => (
    <div>
        <Container>
            <Title>Доход</Title>
        </Container>
    </div>
    
);

export default Debit;